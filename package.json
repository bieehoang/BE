{
  "name": "be",
  "version": "1.0.0",
  "description": "",
  "main": "src/app.js",
  "type": "module",
  "scripts": {
    "start": "nodemon src/C.js",
    "watch": "node-sass --watch src/resouces/scss/app.scss --output src/public/css/ --output-style compressed",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky install"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "method-override": "^3.0.0",
    "node-firefox": "^0.1.0"
  },
  "devDependencies": {
    "express-handlebars": "^6.0.7",
    "husky": "^8.0.3",
    "lint-staged": "^13.1.1",
    "mongoose": "^6.9.1",
    "mongoose-slug-generator": "^1.0.4",
    "morgan": "^1.10.0",
    "node-sass": "^8.0.0",
    "nodemon": "^2.0.20",
    "prettier": "^2.8.4"
  },
  "lint-staged": {
    "src/**/*.{js,scss}": "prettier  --semi --single-quote --trailing-comma all --tab-width 4 --write"
  }
}
